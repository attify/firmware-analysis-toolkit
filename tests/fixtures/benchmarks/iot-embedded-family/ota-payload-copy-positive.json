{
  "id": "ota-payload-copy-positive",
  "kind": "positive",
  "family": "bounds-size-arithmetic",
  "language": "c",
  "file_name": "ota_update.c",
  "compile_args": [
    "clang",
    "-x",
    "c",
    "-std=c11"
  ],
  "reference_symbols": [
    "ota_apply_chunk"
  ],
  "surface_candidate": "ota_apply_chunk_variant",
  "variant_candidate": "ota_apply_chunk_neighbor",
  "baseline_candidates": [
    "ota_apply_chunk_variant"
  ],
  "source": "typedef unsigned int uint32_t;\ntypedef unsigned long size_t;\nvoid* malloc(size_t);\nvoid memcpy(void*, const void*, size_t);\nvoid flash_write(uint32_t, const void*, size_t);\n\ntypedef struct { uint32_t offset; uint32_t length; const void* data; } ota_chunk_t;\n\nvoid ota_apply_chunk(ota_chunk_t* chunk, uint32_t flash_base)\n{\n  void* buf = malloc(chunk->length);\n  memcpy(buf, chunk->data, chunk->length);\n  flash_write(flash_base + chunk->offset, buf, chunk->length);\n}\n\nvoid ota_apply_chunk_variant(ota_chunk_t* chunk, uint32_t flash_base)\n{\n  void* buf = malloc(chunk->length);\n  memcpy(buf, chunk->data, chunk->length);\n  flash_write(flash_base + chunk->offset, buf, chunk->length);\n}\n\nvoid ota_apply_chunk_neighbor(ota_chunk_t* chunk, uint32_t flash_base)\n{\n  void* buf = malloc(chunk->length + 16);\n  memcpy(buf, chunk->data, chunk->length);\n  flash_write(flash_base + chunk->offset, buf, chunk->length);\n}\n"
}
